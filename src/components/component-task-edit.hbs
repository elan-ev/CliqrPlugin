{{#if editable}}
    {{button "edit" "Bearbeiten" icon="edit"}}
    {{fab "edit" "Bearbeiten" "edit"}}
{{else}}
    {{button "copy-edit" "Bearbeiten" icon="exclaim-circle" color="red"}}
    {{fab "copy-edit" "Bearbeiten" "edit" color="red"}}
{{/if}}
