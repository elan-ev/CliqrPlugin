<header class="cliqr--page-header">
    {{> breadcrumb breadcrumb}}

    <h1>Abstimmungen vergleichen</h1>
</header>

<main>
    {{#with votingA}}
        <section class="cliqr--voting-side-a">
            <nav>
                <a href="#voting/{{id}}">
                    Abstimmung {{#if start}}{{moment start}}{{/if}}
                </a>
            </nav>
            <main></main>
        </section>
    {{/with}}

    {{#with votingB}}
        <section class="cliqr--voting-side-b">
            <nav>
                <a href="#voting/{{id}}">
                    Abstimmung {{#if start}}{{moment start}}{{/if}}
                </a>
            </nav>
            <main></main>
        </section>
    {{/with}}
</main>
